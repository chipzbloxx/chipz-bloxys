const products = [

  {
    name: "Crimson Limited",
    cat: "Limiteds",
    icon: "💎",
    desc: "A premium limited-style collectible.",
    price: "View Item"
  },

  {
    name: "Shadow Limited",
    cat: "Limiteds",
    icon: "🕶️",
    desc: "Dark, clean and made to stand out.",
    price: "View Item"
  },

  {
    name: "Game Drop Pack",
    cat: "Game Items",
    icon: "🎮",
    desc: "A featured game-item bundle.",
    price: "View Item"
  },

  {
    name: "XP Boost Item",
    cat: "Game Items",
    icon: "⚡",
    desc: "A community-favorite game item.",
    price: "View Item"
  },

  {
    name: "Chipz Hoodie",
    cat: "Clothing",
    icon: "🧥",
    desc: "Black and red Chipz Bloxys fit.",
    price: "View Item"
  },

  {
    name: "Bloxys Tee",
    cat: "Clothing",
    icon: "👕",
    desc: "Simple Roblox-inspired streetwear.",
    price: "View Item"
  }

];


const grid = document.querySelector("#products");

const search = document.querySelector("#search");

const empty = document.querySelector("#empty");


let filter = "All";


function render() {

  const q = search.value.toLowerCase();


  const shown = products.filter(

    p =>

      (filter === "All" || p.cat === filter)

      &&

      (
        `${p.name} ${p.cat} ${p.desc}`
      )
      .toLowerCase()
      .includes(q)

  );


  grid.innerHTML = shown.map(

    p => `

      <article class="product">

        <div class="product-img">
          ${p.icon}
        </div>

        <div class="product-body">

          <div class="tag">
            ${p.cat.toUpperCase()}
          </div>

          <h3>
            ${p.name}
          </h3>

          <p>
            ${p.desc}
          </p>

          <div class="price">
            ${p.price}
          </div>

        </div>

      </article>

    `

  ).join("");


  empty.hidden = shown.length !== 0;
}


search.addEventListener(

  "input",

  () => {

    filter = "All";

    render();

  }

);


document
  .querySelectorAll(".category")
  .forEach(

    button => {

      button.addEventListener(

        "click",

        () => {

          filter = button.dataset.filter;

          document
            .querySelector("#marketplace")
            .scrollIntoView({
              behavior: "smooth"
            });

          render();

        }

      );

    }

  );


render();
